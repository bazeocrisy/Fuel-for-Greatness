/* Single source of truth for the app build stamp.
   Both the child wizard and the Parent Portal read this, so the two footers
   can never drift apart again. Bump it here and nowhere else. */
window.FFG_BUILD = '2026.08.09-19';
